<?xml version="1.0" encoding="UTF-8"?>
<beans
	xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:sec="http://www.springframework.org/schema/security"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
						http://www.springframework.org/schema/beans/spring-beans.xsd
						http://www.springframework.org/schema/security 
						http://www.springframework.org/schema/security/spring-security.xsd">

	<!-- Enable method level Spring Security by annotations -->
	<sec:global-method-security pre-post-annotations="enabled" />

	<sec:http
		pattern="/login.xhtml"
		security="none" />

	<sec:http auto-config="true">

		<sec:intercept-url
			pattern="/faces/**"
			access="authenticated" />

		<sec:intercept-url
			pattern="/javax.faces.resource/**"
			access="permitAll" />

		<sec:intercept-url
			pattern="/resources/**"
			access="permitAll" />

		<!-- Define login page for Spring Security -->
		<sec:form-login
			login-page="/login.xhtml"
			default-target-url="/faces/home.xhtml"
			always-use-default-target="true" />

		<!-- Define logout properties for Spring Security -->
		<sec:logout
			invalidate-session="true"
			delete-cookies="true"
			logout-success-url="/" />

		<!-- Disable CSRF (won't work with JSF) -->
		<sec:csrf disabled="true" />

		<!-- ensure last HTTP POST request is saved -->
		<!-- <sec:request-cache ref="org.springframework.security.web.savedrequest.HttpSessionRequestCache" /> -->

	</sec:http>

	<!-- Set customUserDetailsService class as the authentication Manager for Spring Security -->
	<sec:authentication-manager alias="authenticationManager">
		<sec:authentication-provider user-service-ref="userService">
			<sec:password-encoder hash="md5" />
		</sec:authentication-provider>
	</sec:authentication-manager>


	<!-- Inject authentication Manager to our LoginBean -->
	<bean
		id="loginBean"
		name="loginBean"
		class="com.giong.managedbean.LoginManagedBean"
		scope="prototype">
		<property
			name="authenticationManager"
			ref="authenticationManager" />
	</bean>

</beans>